#!/usr/bin/env bash
# creload — restart pi in the current workspace

if [ -z "$TMUX" ]; then
    echo "Error: not inside a tmux session."
    exit 1
fi

SERVER=$(echo "$TMUX" | \
    cut -d, -f1 | xargs basename)
SESSION=$(tmux -L "$SERVER" display-message \
    -p '#{session_name}')

# Find the pi pane by title
CPANE=""
COUNT=$(tmux -L "$SERVER" list-panes \
    -t "$SESSION" | wc -l | tr -d ' ')
for i in $(seq 0 $((COUNT - 1))); do
    T=$(tmux -L "$SERVER" display-message \
        -t "$SESSION:0.$i" \
        -p '#{@pane_name}' 2>/dev/null)
    if [ "$T" = "pi" ]; then
        CPANE="$i"
        break
    fi
done

if [ -z "$CPANE" ]; then
    echo "Error: no pi pane found in $SESSION"
    exit 1
fi

# Get working directory
DIR=$(tmux -L "$SERVER" display-message \
    -t "$SESSION:0.$CPANE" \
    -p '#{pane_current_path}')

echo "Restarting pi in pane $CPANE..."

# Kill pane contents and respawn with claude
tmux -L "$SERVER" respawn-pane -k \
    -t "$SESSION:0.$CPANE" \
    "cd '$DIR' && pi"

echo "Done."
