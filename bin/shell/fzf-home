#!/usr/bin/env bash
# fzf from $HOME → open in vim popup
f=$(fzf --reverse)
[ -n "$f" ] || exit 0
tmux run-shell -b \
  "tmux display-popup -E \
  -w 60% -h 100% -x C -y 0 \
  -d \"$HOME\" \
  \"vim '$f'\""
